# PG-75 Triplet context + delta ablation

train=28；dev=14；legacy unknown=8；external known diagnostic=21；device=cuda；OOD threshold=0.250488。

dev recall=1.0；legacy unknown misname=0；independent triplet unknown gate=false。

能力门：`blocked`；training/memory promotion=`false`。
