# PG-104 Probe Binding / 槽位置换消融

状态：`blocked`；valid binding 的已知族召回：`1.0`；标签一致性：`1.0`；误报：`0`。

几何符号消融一致性：`1.0`；无效 binding guarded 弃权率：`1.0`。

PG-69 新实现未知族严格弃权：`False`；raw 未知误命名：`108`。

组合式 Rule IR 原子顺序不变：`True`；候选等待 typed oracle 比率：`1.0`；候选可提升数：`0`。

binding 只承诺安全探针 ID/版本，不保存原始探针值；训练和长期记忆均关闭。
