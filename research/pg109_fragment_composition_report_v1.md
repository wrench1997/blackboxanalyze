# PG-109 Rule Fragment Composition / 原子片段组装

状态：`passed_fragment_composition_diagnostic`；episode：`289`；已知效果组装召回：`1.0`；族外/decoy 弃权率：`1.0`。

跨样本重组：`8`；重组有效率：`1.0`；负例硬门弃权率：`1.0`。

该 Rule IR 只由可复用原子组成、不可执行、必须等待 typed oracle；训练集和长期记忆关闭。
