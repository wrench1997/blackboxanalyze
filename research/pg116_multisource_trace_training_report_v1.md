# PG-116 多源本地轨迹训练

两种本地目标 profile 通过 GET/POST + fresh reset 采集，模型输入不含 oracle/family/raw 值。

- 设备：`cuda`；参数量：`4612`；训练/开发平衡行：`264/264`。
- PG-114 盲测族外正例召回：`1.0`；decoy 误接受：`0`；未知弃权：`1.0`。
- 逐步准确率：`1.0`；该指标用于暴露中间状态混淆，不隐藏。
- checkpoint 仅用于实验，不自动进入长期记忆。
