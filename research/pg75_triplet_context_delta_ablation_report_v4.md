# PG-75 Triplet context + delta ablation

train=28；dev=14；legacy unknown=8；independent unknown triplet=12；external known diagnostic=21；device=cuda；OOD threshold=0.250488。

dev recall=1.0；legacy unknown misname=0；independent unknown misname=0。

能力门：`passed`；training/memory promotion=`false`。
