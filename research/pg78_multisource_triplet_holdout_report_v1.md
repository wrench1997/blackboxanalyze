# PG-78 多实现多族 triplet holdout

cases=270；implementations=5；families=9；GET/POST={'GET': 135, 'POST': 135}；screen missing=162。

known recall=0.0；false accepts=0；能力门=`blocked`。

training/memory promotion=`false`。
