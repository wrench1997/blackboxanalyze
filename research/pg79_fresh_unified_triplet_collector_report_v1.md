# PG-79 Fresh unified triplet collector

triplets=270；positive=240；typed negatives=540；negative probe positives=0；sources=5；families=9；GET/POST={'GET': 135, 'POST': 135}。

硬门：`blocked`；training/memory promotion=`false`。
