# PG-208 Pikachu typed payload loop

device=cuda; crawl surfaces=112; unique entries=112; active routes=13
GET routes=22; POST routes=4; candidate sends=12
DOM dual agreement=8; unknown SQL abstain=14

Only crawl-observed safe fields were replayed on fresh pinned loopback containers. SQL backend oracle is unavailable and remains abstain. No vulnerability claim is made.
