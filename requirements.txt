fastapi==0.128.2
uvicorn[standard]==0.48.0
pydantic==2.13.4
pytest==9.0.2
httpx==0.28.1
numpy>=1.26,<3
# PG-132 uses the open-source Hugging Face tokenizer backend for canonical
# Rule IR pair tokens.  The embedding weights remain an explicitly audited
# PyTorch matrix (fresh by default; optional local checkpoint).
tokenizers>=0.19,<0.20
